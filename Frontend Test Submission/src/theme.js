import { createTheme } from "@mui/material/styles";

export default createTheme({
  palette: {
    primary: { main: "#4285F4" },
    secondary: { main: "#F4B400" },
  },
  typography: {
    h2: { fontWeight: 600, marginBottom: "1rem" },
  },
});
